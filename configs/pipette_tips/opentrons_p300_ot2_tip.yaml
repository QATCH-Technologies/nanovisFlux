# TODO: name/rename this to the tip's actual product name, and rename this
# file to match (keep robot.yaml's tips: reference in sync -- see
# config/loader.py's _check_name_matches, which enforces it).
#
# NOTE: scripts/calibrate_pipette.py's --tip-name defaults to "Opentrons
# OT-2 300ul no Filter" with a --tip-length-mm fallback of 59.0mm, used
# whenever the given --tip-name isn't found in robot.tips (see that
# script's argparse setup). That 59.0mm also matches the worked z_zero
# derivation in configs/calibration.yaml's comment. This file's 51.7mm is
# a DIFFERENT number for what may be the same physical tip -- unresolved;
# confirm which is correct before relying on either for a real run (a
# mismatch here is exactly the kind of tip-length mix-up documented in
# calibration.yaml's z_zero comment).
#
# length_mm shifts every Z target once this tip is on.
#
# TODO brand: the NOTE above already suspects this is an Opentrons tip --
# confirm and fill in "Opentrons" once the length_mm discrepancy is
# resolved (don't want to assert brand ahead of confirming which physical
# tip this file actually describes).
name: opentrons_p300_ot2_tip
brand: Opentrons
length_mm: 51.7
max_volume_ul: 300
