# Same measured reservoir plate as aspirate_plate.yaml -- see that file's
# header for the footprint/well/clearance derivation.
name: dispense_plate
well_geometry:
  shape: rectangular
  width_mm: 120.9
  length_mm: 78.5
  depth_mm: 18.0
  bottom: flat
  bottom_clearance_mm: 1.5
  max_volume_ul: 170832
wells:
  A1: { x: 61.5, y: 40.35, z: 19.1 }
